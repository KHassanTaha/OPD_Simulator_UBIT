using Avalonia.Controls;

namespace OpdSimulator.App.Views;

/// <summary>Code-behind for <see cref="ChartsPanel"/> (no logic beyond the XAML bindings).</summary>
public partial class ChartsPanel : UserControl
{
    /// <summary>Creates the panel.</summary>
    public ChartsPanel()
    {
        InitializeComponent();
    }
}